# DeepDRAC — Python Dependencies
#
# Install core dependencies:
#   pip install -r requirements.txt
#
# Then install PyTorch + PyTorch Geometric matching your system:
#   https://pytorch.org/get-started/locally/
#   https://pytorch-geometric.readthedocs.io/en/latest/install/installation.html
#
# Verified environment: Python 3.10, PyTorch 2.6.0+cu124, PyG 2.8.0

# ── Core Numerical & Data ────────────────────────────────────────────────────
numpy>=1.24.0,<2.0  # grakel requires numpy < 2.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.10.0

# ── Graph & Network ─────────────────────────────────────────────────────────
networkx>=3.1
python-louvain>=0.16
igraph>=0.10.0
leidenalg>=0.10.0
mlxtend>=0.22.0

# ── Graph Kernels (for pre-train triple generation) ─────────────────────────
grakel>=0.1.10

# ── Deep Learning (install manually matching your CUDA/PyTorch version) ─────
# torch>=2.0.0
# torch_geometric>=2.3.0
# torch_scatter
# torch_sparse
# torch_cluster
# torch_spline_conv

# ── Incremental Clustering ──────────────────────────────────────────────────
incdbscan>=0.1.0

# ── Experiment Tracking (optional) ──────────────────────────────────────────
wandb>=0.15.0
swanlab>=0.3.0

# ── Visualization & Utilities ───────────────────────────────────────────────
matplotlib>=3.7.0
tqdm>=4.65.0

# ── Jupyter (for notebooks) ─────────────────────────────────────────────────
jupyter>=1.0.0
ipykernel>=6.0.0
